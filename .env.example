# Next.js Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local or any .env file with real values

# Site Configuration (Client-side - must be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_PORTFOLIO_PASSWORD=

# Email Configuration (Server-side - API routes only)
RESEND_API_KEY=
CONTACT_EMAIL=

# Optional: Bundle Analysis
# Set to 'true' to enable bundle analysis during build
ANALYZE=
